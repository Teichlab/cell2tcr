[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "cell2tcr"
authors = [{name = "Lisa M Dratva", email = "LMD76@cam.ac.uk"}]
readme = "README.md"
license = {file = "LICENSE"}
dynamic = ["version", "description"]
dependencies = [
    "numpy",
    "pandas",
    "scanpy",
    "celltypist",
    "tcrdist3",
    "igraph",
    "leidenalg",
    "levenshtein",
    "matplotlib",
    "logomaker",
]
requires-python = ">=3.8"

[project.urls]
Home = "https://github.com/Teichlab/cell2tcr"